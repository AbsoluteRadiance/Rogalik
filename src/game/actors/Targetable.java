package game.actors;

/**
 * A marker interface for signalling targetability.
 * Contains no methods, is only used to identify
 * entities which are targetable by other entities - namely the workers/player.
 */
public interface Targetable {
}
